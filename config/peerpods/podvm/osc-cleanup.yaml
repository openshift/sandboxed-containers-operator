apiVersion: batch/v1
kind: Job
metadata:
  name: osc-podvm-configuration-clean
  namespace: openshift-sandboxed-containers-operator
spec:
  parallelism: 1
  completions: 1
  backoffLimit: 1
  template:
    metadata:
      name: osc-podvm-configuration-clean
    spec:
      containers:
        - name: clean
          image: registry.redhat.io/openshift-sandboxed-containers/osc-podvm-builder-rhel9:1.7.0
          imagePullPolicy: Always
          securityContext:
            runAsUser: 0 # needed for container mode dnf access
          envFrom:
            - secretRef:
                name: ocp-libvirt-secret 
            - secretRef:
                name: peer-pods-secret
            - configMapRef:
                name: libvirt-podvm-image-cm
                optional: true
          command: ["/podvm-builder.sh", "config-cleanup"]
      restartPolicy: Never
